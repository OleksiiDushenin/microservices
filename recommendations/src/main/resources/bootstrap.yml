spring:
  application:
    name: recommendations
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://config-server:8888
      enabled: true
  zipkin:
    baseUrl:  http://zipkin:8762

